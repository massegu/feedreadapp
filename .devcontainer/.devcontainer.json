{
  "name": "feedreadapp",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [3000, 8000],
  "postCreateCommand": "sudo service postgresql start && npm install --prefix frontend && pip install -r backend/requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint",
        "ms-azuretools.vscode-docker"
      ]
    }
  },
  "remoteEnv": {
    "FASTAPI_PORT": "8000",
    "REACT_PORT": "3000"
  }
}
